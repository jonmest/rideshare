[package]
name = "kafka"
version = "0.1.0"
edition = "2024"

[dependencies]
ride_events = { path = "../events" }

prost = "0.14.3"
prost-types = "0.14.3"
tokio = { version = "1", features = ["full"] }
rdkafka = { version = "0.36", features = ["tokio"] }

[build-dependencies]
prost-build = "0.14.3"
